# Concern2Care Environment Variables for Railway

# Admin DeepSeek API Key (for demo functionality)
AdminDeepSeekAPIKey=sk-baac06039caa4c7a887151e8952e34a3

# Database (Railway will auto-inject DATABASE_URL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Custom domain configuration
# FRONTEND_URL=https://your-domain.com

# Optional: Email configuration (for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_email_password

# Security (Railway will auto-generate if not provided)
# JWT_SECRET=your_jwt_secret_here
